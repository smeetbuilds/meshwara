node_modules
.output
.dist
dist
.vite
.env
.DS_Store
tests/model-pipeline/.generated-validation.glb
tests/model-pipeline/.generated-validation.gltf

# Generated public registry. Production builds recreate it from the SHA-manifested downloads.
public/registry/v1.json

# Generated crawl metadata. Builds recreate these using MESHVARA_SITE_URL.
public/robots.txt
public/sitemap.xml

# Materialized third-party source bytes (verified locally, never committed)
staging/materialized/
playwright-report/
test-results/
.playwright/

# Same-origin codec runtime emitted from the exact pinned three package.
# README stays tracked; build/dev/release recreate these bytes deterministically.
public/codecs/draco/
public/codecs/basis/
public/codecs/manifest.json

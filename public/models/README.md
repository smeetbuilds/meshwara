# Public modeled assets

Each publishable modeled asset gets its own directory containing `manifest.json` and desktop/tablet/mobile glTF or GLB tiers.

No placeholder human/character is published here. A model only enters this directory when its actual redistributable files and QA evidence are ready.
